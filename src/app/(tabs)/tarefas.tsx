import { View, Text } from "react-native";

export default function Tarefas() {
  return (
    <View style={{flex:1, justifyContent:"center", alignItems:"center"}}>
      <Text>Lista completa de tarefas 🚀</Text>
    </View>
  );
}